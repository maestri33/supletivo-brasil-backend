# Student

- **VMID:** 140
- **Container:** student
- **IP:** 10.10.10.140

## Descrição

Container para serviço de estudantes. **Nenhum código-fonte encontrado** no filesystem.

- Apenas SSH (porta 22) e serviços de sistema (systemd, postfix) rodando
- Sem diretórios de aplicação em `/root`, `/opt`, `/home`, `/srv` ou `/var/www`
- Possivelmente container provisionado mas ainda não deployado, ou consumer de serviços externos

## Stack

- **Status:** Indeterminado (sem código)
