# Hub

- **VMID:** 142
- **Container:** hub
- **IP:** 10.10.10.142

## Descrição

Container para serviço hub/central. **Nenhum código-fonte encontrado** no filesystem.

- Apenas SSH (porta 22) e serviços de sistema rodando
- Sem diretórios de aplicação em `/root`, `/opt`, `/home`, `/srv` ou `/var/www`
- Possivelmente container provisionado mas ainda não deployado

## Stack

- **Status:** Indeterminado (sem código)
